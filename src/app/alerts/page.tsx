import { PlaceholderScreen } from "@/components/shared/PlaceholderScreen";

export default function AlertsPage() {
  return (
    <PlaceholderScreen
      title="Alerts"
      description="Localised official warnings for your current area and Watch Zones — always visually separate from community reports. No disaster mode."
      phase="Phase 6 (Localised warnings)"
    />
  );
}
