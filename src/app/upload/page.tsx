import { PlaceholderScreen } from "@/components/shared/PlaceholderScreen";

export default function UploadPage() {
  return (
    <PlaceholderScreen
      title="Upload"
      description="Resumable weather-footage uploads with location-privacy controls, weather-type tagging, and processing status."
      phase="Phase 5 (Upload and media pipeline)"
    />
  );
}
