import { PlaceholderScreen } from "@/components/shared/PlaceholderScreen";

export default function ProfilePage() {
  return (
    <PlaceholderScreen
      title="Profile"
      description="Identity, contributions, saved content, Watch Zones, preferences, privacy and account settings."
      phase="Phase 4 (Authentication, profiles and Watch Zones)"
    />
  );
}
