// Phase 0 placeholder — proves the TypeScript toolchain (tsconfig, strict mode,
// path aliases) is wired up correctly before any real application code exists.
// Delete this file once Phase 1 adds real source under src/.
export const PHASE = 0 as const;
